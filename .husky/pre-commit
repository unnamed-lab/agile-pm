#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Temporarily disabled for Phase 1 commits
echo "Husky pre-commit hook disabled for Phase 1"
# pnpm --filter api lint && pnpm --filter web lint
# pnpm --filter api build && pnpm --filter web build
